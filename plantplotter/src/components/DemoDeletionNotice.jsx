export default function DemoDeletionNotice({ className = '' }) {
  return (
    <p className={`text-sm text-gray-600 ${className}`}>
      Showcase records stay available for everyone. You can create and delete your own demo records.
    </p>
  );
}
